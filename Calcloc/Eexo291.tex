Soit $(u_n)_{n\in\N})$ une suite de nombres strictement positifs telle que $(\ln u_n)_{n\in\N})$ converge. La suite $(u_n)_{n\in\N})$ converge-t-elle? Si oui est-ce à cause de la continuité de la fonction logarithme ou de la fonction exponentielle ? \bigskip