Soit $A\in\mathcal M_p(\K)$, $\lambda\in \K$, $i$ et $j$ deux entiers entre $1$ et $p$. Soit $P\in GL_p(\K)$ telle que $PA$ est obtenue à partir de $A$ en ajoutant $\lambda$ fois la ligne $j$ à la ligne $i$ sans changer les autres (codage $L_i \leftarrow L_i +\lambda L_j$).\newline
Comment est obtenue $A\mathstrut^t\!P$ à partir de $A$ ? (indiquer seulement le codage) \bigskip